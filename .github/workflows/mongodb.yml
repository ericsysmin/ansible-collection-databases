name: "ericsysmin.databases.mongodb"
on:
  push:
    paths:
      - "roles/mongodb/**"
      - "molecule/mongodb/**"
      - ".github/workflows/mongodb.yml"
  pull_request:
    paths:
      - "roles/mongodb/**"
      - "molecule/mongodb/**"
      - ".github/workflows/mongodb.yml"
jobs:
  mongodb:
    runs-on: ubuntu-18.04
    env:
      PY_COLORS: 1
      ANSIBLE_FORCE_COLOR: 1
    strategy:
      fail-fast: true
      matrix:
        molecule_distro:
          - { "distro": "ubuntu-16.04", "command": "/sbin/init" }
          - { "distro": "ubuntu-18.04", "command": "/lib/systemd/systemd" }
          - { "distro": "ubuntu-20.04", "command": "/lib/systemd/systemd" }
          - { "distro": "debian-9", "command": "/lib/systemd/systemd" }
          - { "distro": "debian-10", "command": "/lib/systemd/systemd" }
        collection_role:
          - mongodb
    steps:
      - name: Check out code
        uses: actions/checkout@v1

      - name: List dir
        run: ls -lart

      - name: Set up Python 3.8
        uses: actions/setup-python@v1
        with:
          python-version: 3.8

      - name: Install dependencies
        run: |
          sudo apt install apt-transport-https ca-certificates curl software-properties-common
          curl -fsSL https://get.docker.com -o get-docker.sh
          sudo sh get-docker.sh
          python -m pip install --upgrade pip
          pip install ansible molecule ansible-lint docker molecule-docker

      - name: Run role tests
        run: >-
          ansible-lint roles/${{ matrix.collection_role }} &&
          molecule --version &&
          ansible --version &&
          MOLECULE_COMMAND=${{ matrix.molecule_distro.command }}
          MOLECULE_DISTRO=${{ matrix.molecule_distro.distro }}
          molecule --debug test -s ${{ matrix.collection_role }}
